# API base URL — points at the backend
VITE_API_BASE_URL=http://localhost:3000/api

# Order / Pricing (values in ₹) — keep in sync with the backend
VITE_MIN_ORDER_VALUE=350
VITE_DELIVERY_CHARGE=50
VITE_FREE_DELIVERY_MIN=500
